@{
    ViewData["Title"] = "Home";
}

<div class="container mt-5">
    <div class="border border-black rounded shadow-sm p-4 text-center">
        <h2 class="text-primary mb-3">
            <i class="bi bi-journal-code me-2"></i>Welcome
        </h2>

        <p class="fs-5 mb-0">
            Upskilling and learning <span class="fw-semibold text-decoration-underline">ASP.NET Core</span> using
            <span class="fw-semibold">Bootstrap</span>, <span class="fw-semibold">Bootswatch</span>, and <span class="fw-semibold">Bootstrap Icons</span>.
        </p>
    </div>
</div>
